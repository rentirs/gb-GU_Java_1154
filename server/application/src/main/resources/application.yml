server:
  servlet:
    context-path: /api/v1

spring:
  datasource:
    username: gb
    password: geekbrains
    url: jdbc:mysql://192.168.1.100:3306/mall_inventory
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

  mail:
    host: smtp.google.com
    port: 587
    username: <changeit>
    password: <changeit>
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
